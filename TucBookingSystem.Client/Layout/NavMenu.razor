@using TucBookingSystem.Client.Services
@inject UserStateService UserState
@inject NavigationManager Nav
@implements IDisposable

<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">
            <img src="/tuclogo-bg.png" class="brand-logo" alt="TUC" />
            <div class="brand-text">
                <span class="brand-tuc">TUC</span>
                <span class="brand-sub">Bokningssystem</span>
            </div>
        </a>
    </div>
</div>

<input type="checkbox" title="Navigation menu" class="navbar-toggler" autocomplete="off" @key="@Nav.Uri" />

<div class="nav-scrollable" onclick="document.querySelector('.navbar-toggler').checked = false">
    <hr class="nav-divider" />
    <nav class="nav flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <i class="bi bi-house-door-fill nav-icon"></i> Hem
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="bookings">
                <i class="bi bi-calendar-check-fill nav-icon"></i> Bokningar
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="rooms">
                <i class="bi bi-door-open-fill nav-icon"></i> Rum
            </NavLink>
        </div>

        @if (UserState.IsAdmin)
        {
            <div class="nav-item px-3">
                <NavLink class="nav-link" href="admin">
                    <i class="bi bi-shield-fill-check nav-icon"></i> Admin
                </NavLink>
            </div>
        }

        @if (UserState.IsLoggedIn)
        {
            <div class="nav-item px-3">
                <NavLink class="nav-link" href="min-profil">
                    <i class="bi bi-person-fill nav-icon"></i> Min profil
                </NavLink>
            </div>
        }

        <div class="nav-item px-3 logout-item">
            @if (UserState.IsLoggedIn)
            {
                <button class="nav-link logout-btn" @onclick="Logout">
                    <i class="bi bi-box-arrow-right nav-icon"></i> Logga ut
                </button>
            }
            else
            {
                <button class="nav-link logout-btn" @onclick="Login">
                    <i class="bi bi-box-arrow-in-right nav-icon"></i> Logga in
                </button>
            }
        </div>
    </nav>
</div>

@code {
    private bool _hasLoaded = false;

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender && !_hasLoaded)
        {
            await UserState.LoadStateAsync();
            _hasLoaded = true;
            StateHasChanged();
        }
    }

    protected override void OnInitialized()
    {
        UserState.OnChange += StateHasChanged;
    }

    private async void Logout()
    {
        await UserState.LogoutAsync();
        Nav.NavigateTo("/login", forceLoad: true);
    }

    private void Login()
    {
        Nav.NavigateTo("/login", forceLoad: true);
    }

    public void Dispose()
    {
        UserState.OnChange -= StateHasChanged;
    }
}
